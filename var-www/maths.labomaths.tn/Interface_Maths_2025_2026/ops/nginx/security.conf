# À inclure dans votre bloc server {} Nginx (ex: include /etc/nginx/snippets/security.conf;)

# Politique stricte par défaut
add_header Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; font-src 'self' data:; connect-src 'self'; object-src 'none'; base-uri 'self'; form-action 'self'; frame-ancestors 'none'; upgrade-insecure-requests" always;

# Politique de référent
add_header Referrer-Policy "strict-origin-when-cross-origin" always;

# Interdiction d'iframe
add_header X-Frame-Options "DENY" always;

# Empêcher le sniffing contenu
add_header X-Content-Type-Options "nosniff" always;

# Permissions-Policy (exemples, tout désactiver)
add_header Permissions-Policy "geolocation=(), microphone=(), camera=()" always;

# HSTS (activer uniquement si HTTPS partout et après validation)
# add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;

# Cache statique (optionnel, ajuster selon besoins)
# location ~* \.(css|js|svg|webp|avif|png|jpg|jpeg|gif|woff2?)$ {
#   add_header Cache-Control "public, max-age=31536000, immutable";
# }

